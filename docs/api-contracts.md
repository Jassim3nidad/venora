# API Contracts (Deprecated Planning Document)

Status: **DEPRECATED**.

This path previously contained proposed frontend contracts that conflicted with
implemented response shapes, routes, booking statuses, and validation. It is
retained so old links do not break.

Use the code-backed [API documentation](api/README.md),
[endpoint inventory](api/endpoint-inventory.md),
[Server Actions](api/server-actions.md), and [OpenAPI document](api/openapi.json).
New contract changes must update those sources and their validators.
